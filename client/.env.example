# Public-only frontend configuration.
# Copy this file to `client/.env.local` (git-ignored) and adjust if needed.
# This value is baked into the client bundle at build time and is safe to expose.
# Never put secrets (JWT signing keys, database passwords, private keys) here.
VITE_API_BASE_URL=http://localhost:8080/api/v1
